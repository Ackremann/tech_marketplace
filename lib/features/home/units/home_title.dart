part of '../view.dart';

class _HomeTitle extends StatelessWidget {
  const _HomeTitle({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ViewTitle(title: 'Home');
  }
}
