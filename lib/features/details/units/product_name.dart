part of '../view.dart';

class _ProductName extends StatelessWidget {
  const _ProductName({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ViewTitle(
      title: 'iphone 13',
      padding: 0,
    );
  }
}
