part of '../view.dart';

class _SearchText extends StatelessWidget {
  const _SearchText({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ViewTitle(
      title: 'Search',
      padding: 20,
    );
  }
}
