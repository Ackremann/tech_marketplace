part of '../view.dart';

class _FavoriteTitle extends StatelessWidget {
  const _FavoriteTitle({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ViewTitle(title: 'Favorite');
  }
}
